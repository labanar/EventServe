using System;
using System.Collections.Generic;
using System.Text;

namespace EventServe.Subscriptions.Enums
{
    public enum SubscriptionConnectionStatus
    {
        Idle = 0,
        Connected = 1,
        Disconnected = 2
    }
}
